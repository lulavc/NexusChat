from setuptools import setup, find_packages

setup(
    name="NexusChat",  # Updated project name from Ollama Chat to NexusChat
    version="0.91b",   # (Or your current version)
    packages=find_packages(),
    # ... other metadata and configuration ...
) 